/* eslint-disable react/no-multi-comp */
import React from 'react';
import { WrappedFormUtils } from 'antd/lib/form/Form';

export const EditableTableContext = React.createContext({} as WrappedFormUtils);
